# Python >= 3.11 required (scikit-learn 1.8+ constraint)
torch==2.9.1
pyyaml>=6.0
numpy==2.2.6
scikit-learn>=1.8.0
matplotlib>=3.10.7
neuraloperator==2.0.0
pin==3.0.0   # macOS and Linux only — Windows users: conda install pinocchio -c conda-forge
jupyter>=7.5.5
